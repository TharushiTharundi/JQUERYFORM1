$( "#btn" ).click(function() {
  //alert( "Handler for .click() called." );


 
  let username =$("#username").val(); // get username value
  let detailsname = $("#detailsname"); // get value field for set the value 
  $(detailsname).text("Username :"+""+username); // show and set value 
  $("#username").val("");

 
   let age =$("#age").val(); // get age value
   let numage = parseInt(age); // convert to int
   let detailsage = $("#detailsage"); //get value field for set the data
   $(detailsage).text("User age :"+""+numage); // show and set value 
   $("#age").val("");// clear



   let weight =$("#weight").val();
   let height =$("#height").val();
   let result =$("#resultbmi");

   let bmifinal = (weight/((height*height)/10000)).toFixed(2);
   if( bmifinal < 18.6){
   // alert("sdkbvshv");
   
    $(result).text("UNDER WEIGHT :" +"  "+ bmifinal);
    
  }else if (bmifinal >= 18.6 &&  bmifinal < 24.9){
   // alert('hdakn ad bbbbbbbbbbbbbbbbbbbbbbbbbbbb');
    
     $(result).text("NORMAL WEIGHT :" +"  "+ bmifinal);

      
  }else{
        
        $(result).text("OVER WEIGHT  :" +"  "+ bmifinal);
            
  }
   
  $("#weight").val("");
  $("#height").val("");
   

});